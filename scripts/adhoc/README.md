# Ad Hoc Scripts

One-off maintenance, migration, and debugging scripts that are not part of normal runtime or CI execution.

- `apex/` contains anonymous Apex snippets.
- `python/` contains local patch and metadata helper scripts.
- `powershell/` contains Windows-specific helper scripts.
