# Artifacts

Generated files that are useful for audits, debugging, or historical review, but are not runtime entrypoints.

- `reports/` contains scanner, deployment, and test result exports.
- `salesforce/` contains extracted or unsupported Salesforce metadata snapshots that are intentionally outside `force-app`.
